# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Environment / secrets — NEVER commit these
# three-ws-core and future integrations may write .env files;
# all variants are excluded here to prevent accidental secret exposure.
.env
.env.*
.env.local
.env.*.local

# Editor / OS artefacts
.DS_Store
*.local
Thumbs.db

# Vite cache
.vite/
