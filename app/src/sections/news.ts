/**
 * sections/news.ts
 * ----------------
 * News / updates section.
 *
 * TODO (next wave):
 *   - Fetch articles from src/data/news.json.
 *   - Render a card grid with title, date, and excerpt.
 *   - NOTE: All runtime fetches must use assetUrl() from src/lib/helpers.ts
 *     to resolve correctly under the /tidlor-reborn/ GitHub Pages subpath.
 *
 * Example fetch pattern:
 *   import { assetUrl } from '../lib/helpers'
 *   const res = await fetch(assetUrl('data/news.json'))
 *   const articles = await res.json()
 */

export function initNews(): void {
  // TODO: implement
}
